node_modules
dist
.sites-runtime
coverage
*.local
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Environment/tooling metadata, not part of the app
.openai/

# Intermediate source images (unused by production code, kept locally only). They live
# outside public/, because Vite copies public/ into dist wholesale (V1.2 Slice 8).
asset-sources/
public/img/example/

.env
.env.local
.env.*.local